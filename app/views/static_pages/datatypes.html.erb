<div class="jumbotron">
  <table id="example" class="table table-striped table-bordered" cellspacing="0" width="100%">
      <thead>
          <tr>
              <th>uid</th>
              <th>name</th>
              <th>mindate</th>
              <th>maxdate</th>
          </tr>
      </thead>
      <tbody>
          <% @datatypes.each do |datatype| %>
            <tr>
                <td><%= datatype.d_id %></td>
                <td><%= datatype.name %></td>
                <td><%= datatype.mindate %></td>
                <td><%= datatype.maxdate %></td>
            </tr>
          <% end %>
      </tbody>
  </table>
</div>
<script type="text/javascript">
  $(document).on('ready page:load', function () {
      $('#example').DataTable();
  } );
</script>